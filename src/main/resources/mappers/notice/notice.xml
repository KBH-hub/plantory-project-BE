<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zero.plantory.domain.notice.mapper.NoticeMapper">

    <select id="selectNoticesByReceiver" resultType="com.zero.plantory.global.dto.NoticeDTO">
        SELECT
            notice_id,
            receiver_id,
            target_type,
            target_id,
            content,
            read_flag,
            created_at,
            del_flag
        FROM notice
        WHERE receiver_id = #{receiverId}
          AND del_flag IS NULL
          AND read_flag IS NULL
        ORDER BY created_at DESC
    </select>

    <insert id="insertNotice" parameterType="com.zero.plantory.global.dto.NoticeDTO">
        INSERT INTO notice
        (receiver_id, target_type, target_id, content, read_flag, created_at, del_flag)
        VALUES
            (#{receiverId}, #{targetType}, #{targetId}, #{content}, NULL, NOW(), NULL)
    </insert>

    <update id="updateNoticeReadFlag">
        UPDATE notice
        SET read_flag = NOW()
        WHERE notice_id = #{noticeId}
    </update>

    <update id="deleteAllNotice">
        UPDATE notice
        SET del_flag = NOW()
        WHERE receiver_id = #{receiverId}
          AND del_flag IS NULL
    </update>

</mapper>