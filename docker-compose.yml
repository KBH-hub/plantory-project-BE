version: '3'

services:
  licensingservice:
    image: ostock/licensing-service:0.0.1-SNAPSHOT
    ports:
      - "9000:9000"
    environment:
      SPRING_PROFILES_ACTIVE: dev
    restart: unless-stopped